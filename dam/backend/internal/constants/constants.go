package constants

const (
	// Asset directories (relative to DataPath)
	AssetsDir = "assets"
	ImagesDir = "assets/images"

	// Workspace structure (relative to AssetsPath)
	SunoDir  = "suno/output"
	UdioDir  = "udio/sorted"
	TrashDir = "trash"
)
